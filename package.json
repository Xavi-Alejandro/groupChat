{
  "name": "messengerserver",
  "version": "1.0.0",
  "description": "This is the server for messages",
  "main": "webSocketServer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xavi",
  "license": "ISC",
  "dependencies": {
    "wss": "^3.3.4"
  }
}
